# CENG-T007-P003-EV001

## Baseline

- Base revision: `cefd662d05e4e4c0b363cd6c23ba32eb6a805717`
- Reviewed source revisions:
  - PR 1 checkout upgrade: `0141b878e5561c57966c273f605571df0ecc6f13`
  - PR 5 CodeQL analyze upgrade: `efcd388cbaaf5cdea63550afb80d872fd35f2e75`
  - PR 6 CodeQL init upgrade: `0dbfb6c34bedb8d3a5d535e2d9541a9c883dcb76`
- Protocol lock: `.contract-engineering/protocol.lock.yaml`
- Environment: macOS 14.4, Python 3.9, PyYAML available

## Changes validated

- `actions/checkout` is pinned to `3d3c42e5aac5ba805825da76410c181273ba90b1`
  in the protocol-validation and security workflows.
- Both negative workflow tests replace that current checkout pin with an
  unpinned ref and reject it.
- CodeQL `init` and `analyze` both use
  `cdf488f595d80d6e07e03d4674febd5ab45fa938`.

## Validation results

- Workflow YAML parsing: passed for all workflow files.
- Full action SHA pinning: passed for all workflow files.
- Protocol workflow security invariants and both negative cases: passed.
- Security workflow invariants and both negative cases: passed.
- CodeQL action-version alignment: passed.
- `python3 scripts/validate-contract-records.py`: passed after synchronizing
  the packet and tracker state.
- `git diff --check`: passed.
- Import graph and layer checks: not applicable to workflow-only changes.

## Limitations

The current commit's GitHub-hosted workflow execution is now evidenced by
successful Protocol validation, Security checks, Secret scanning, CodeQL, and
Scorecard runs. GitHub pull-request merge/closure operations remain outside
this packet's scope and are still unresolved.
