# CENG-T016-P003-EV001

## Revision-safe lifecycle validation

- `python3 -m unittest tests.test_transitions -v`: exit 0, 10 tests passed.
- `python3 -m unittest discover -s tests -p 'test_*.py'`: exit 0, 87 tests passed.
- `python3 scripts/validate-contract-records.py --root .contract-engineering`: exit 0.
- `python3 scripts/validate-tracker.py --root .contract-engineering`: exit 0.
- `python3 scripts/render-tracker.py --root .contract-engineering --check`: exit 0.

The coordinator now requires an expected packet revision, supports the documented
early lifecycle transitions, updates tracker and event projections, replays
prepared operations idempotently, and avoids creating the journal directory for
read-only diagnostics.
