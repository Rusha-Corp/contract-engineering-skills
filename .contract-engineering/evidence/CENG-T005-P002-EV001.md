# CENG-T005-P002-EV001

- Added Gitleaks 8.24.3 configuration and a SHA-verified release download for
  local and CI scanning.
- CI scans complete Git history and the working tree, including generated files
  present during the job, with redacted output and no report upload or pull
  request comments.
- Added evidence-policy contract validation, common credential filename
  blocking, secure `.gitignore` defaults, and containment/rotation guidance.
- Added a negative test that builds a synthetic private-key fixture without
  storing or printing the marker in repository source; the scanner rejected it.
- Validation passed: local history scan, local working-tree scan, negative
  fixture scan, YAML parsing for the workflow and evidence policy, and the
  pinned Gitleaks archive checksum.
- The scan initially reported the tracked negative-test marker itself; the
  marker was changed to shell-constructed fragments, after which the
  repository scan passed without adding an allowlist exception.
- No credentials, secret values, or raw scanner findings were recorded.
