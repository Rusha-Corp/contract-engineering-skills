# CENG-T016-P005-EV001

- Packet: `CENG-T016-P005`
- Base revision: `099450d292ca470f58f5ec19d51139a9ac992e1c`
- Implementation revision: `f5448ab`
- Worktree: `/home/dev/contract-engineering-skills-CENG-T016-P005`

## Acceptance criteria

- Worktree metadata is represented in the packet schema and template with path,
  branch, base revision, and cleanup status.
- `reconcile_worktrees.py` parses Git porcelain output and deterministically
  reports registered, unregistered, missing, stale, and drifted worktrees.
- Reconciliation reads packet files and Git output only. It does not write
  packet records, Git refs, or worktrees.

## Validation

| Command | Result |
| --- | --- |
| `python3 -m unittest tests.test_worktrees -v` | exit 0, 3 tests passed |
| `python3 -m unittest discover -s tests -p 'test_*.py'` | exit 0, 99 tests passed |
| `python3 scripts/validate-contract-records.py --root .contract-engineering` | exit 0, contract validation valid |
| `python3 scripts/validate-tracker.py --root .contract-engineering` | exit 0, 28 active rows, 24 archive rows |
| `python3 scripts/reconcile_worktrees.py --root .contract-engineering` | exit 0, current P005 worktree reported as registered |

The live reconciliation also reports older T013/T014 worktrees as
unregistered. This packet intentionally does not delete or mutate them; they
remain for a later reconciliation or cleanup decision.

## Limitations

- No cleanup action is included. Reconciliation is deliberately read-only.
- The current worktree report does not infer ownership for legacy branches that
  do not contain a packet identifier.
