# CENG-T015-P001-EV001

## Baseline

Release preparation starts from clean coordination revision
`824f4970e3d583bf0558dcf40d8de3e0f883b501`, after T014 implementation,
acceptance, integration, archival, and superseded P002 cancellation.
The last verified release remains protocol `2.5.4` at
`80f1c21f9428324e07644484bdfdac3832bb49e9`.

## Release proposal

- Protocol release: `2.6.0`.
- Factory plugin release: `2.3.0`.
- Rollback reference: `80f1c21f9428324e07644484bdfdac3832bb49e9`.
- Publication remains blocked until independent review and final user approval.

## Security controls

The release threat model is recorded at
`.contract-engineering/threat-models/CENG-T015-P001.yaml`. It covers source
tampering, supply-chain compromise, repudiation, unauthorized publication,
prompt injection, tool poisoning, and signing-secret exposure.

## Metadata and local validation

- Protocol release candidate: `2.6.0`.
- Factory plugin release candidate: `2.3.0`.
- Full repository suite: 118 tests passed.
- Markdown links: 32 files passed.
- Design contracts: 1 passed.
- Contract records: 54 packets passed.
- Admission: 0 ready packets remained.
- Tracker and projections: 19 active and 35 archived rows passed.
- Factory plugin validation and reproducibility checks passed.
- Five normative JSON schemas passed metadata checks.
- All workflow action references are full commit SHAs and workflow security
  invariants passed.
- Supplemental non-disclosing secret-pattern scan passed.

## Security limitations

The local environment does not have Gitleaks, CodeQL, or the GitHub
dependency-review and Scorecard services. The repository CI workflows remain
the required checks for those controls. No exception has been requested or
approved, so the release cannot advance to publication until the checks run
and an independent reviewer approves the exact content and attestation.

No remote action, signing action, tag, push, or consumer lock update occurred.

## Independent review

The independent reviewer rejected the candidate because the required CI
security services have not run locally or remotely, the 2.6.0 attestation has
not been created, and the two-person approval gate is incomplete. The packet
is in `Rework`. The next action is to obtain explicit authorization to push
the release-candidate branch for CI-only verification.

## CI dependency correction

The first draft-PR protocol-validation run failed because
`requirements-ci.txt` omitted the imported `jsonschema` dependency. Decision
`CENG-T015-P001-DEC002` adds `jsonschema==4.21.1` and its exact
hash-pinned runtime dependencies. The draft PR must rerun all workflows after
this correction.

The corrected requirements manifest resolves successfully with
`pip install --dry-run --ignore-installed --require-hashes`.
The first corrected CI run then identified the CPython 3.12 `rpds-py` wheel
hash; both the local compatible wheel and CI's CPython 3.12 wheel are now
allowed explicitly.
