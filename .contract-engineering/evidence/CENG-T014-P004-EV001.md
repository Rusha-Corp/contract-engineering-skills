# CENG-T014-P004-EV001

## Baseline

P004 was admitted from coordination revision `3060afd` against approved
design `CENG-T014-DC001`, with dependencies P003 and P011 complete. The
implementation worktree is
`/home/dev/contract-engineering-skills-CENG-T014-P004`.

## Implementation

Added manifest path confinement and SHA-256 validation, offline HTML policy,
SVG safety validation, localhost-only preview serving, fail-closed Mermaid
CLI rendering, and a pinned local runtime marker. Added safe and unsafe
fixtures plus focused regression tests.

## Validation

- Focused preview suite: 7 tests passed.
- Full repository suite: 98 tests passed.
- Design contracts: 1 design passed.
- Contract records: 53 packets passed.
- Admission: 1 ready packet passed.
- Tracker and projection validation passed with 29 active and 24 archived
  rows.
- Safe SVG validation passed and scripted SVG rejection passed.
- Security surface decision: `CENG-T014-P004-DEC001`.

## Limitations

Mermaid CLI rendering remains fail-closed when `mmdc` is unavailable. The
browser-facing local runtime exposes the pinned offline capability marker and
does not claim rendered output without a verified renderer.

## Next action

Run the final packet validation plan, prepare the handoff, and obtain receiver
acceptance before releasing the `design-preview-runtime` lock.
