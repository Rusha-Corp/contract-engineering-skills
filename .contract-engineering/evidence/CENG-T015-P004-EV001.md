# CENG-T015-P004-EV001

## Handoff and lease enforcement validation

- `python3 -m unittest tests.test_handoffs tests.test_leases tests.test_transitions`: exit 0, 18 tests passed.
- `python3 -m unittest discover -s tests -p 'test_*.py'`: exit 0, 95 tests passed.
- `python3 scripts/validate-contract-records.py --root .contract-engineering`: exit 0.
- `python3 scripts/validate-tracker.py --root .contract-engineering`: exit 0.
- `python3 scripts/render-tracker.py --root .contract-engineering --check`: exit 0.

The coordinator now requires accepted revision-bound handoffs for completion and
active fencing leases for high-risk packets. Lease claim, expiry, takeover,
renewal, release, stale-token rejection, and read-only diagnostics are covered
by tests.
