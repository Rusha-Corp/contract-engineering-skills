# CENG-T004-P017-EV001

## Attestation preparation

- Attestation subject source commit: `54e3a33e2ca904c41050b9344b16d809cb26a0ba`.
- Release target: stable `2.2.0`.
- Rollback reference: verified prior release commit
  `575f473a9349c5dfb61df7758b52a5592b2e2915`.
- Artifact hashes were computed from the candidate source tree and match the
  project lock for all six governed skills.
- Publisher identity is the configured GPG identity; private key material is
  not stored in the repository.
- Independent approval is bound to
  `REVIEWER-ACCEPT-CENG-T004-P016-20260831`.

## Validation status

- Contract-record validation: pending final P017 handoff reconciliation.
- Unit and schema checks: pass on the candidate source.
- Local workflow and lock/source checks: pass on the candidate source.
- Gitleaks, CodeQL, dependency review, and OpenSSF Scorecard: hosted checks
  required; not available in this local environment.
- Signed tag, remote publication, and hosted stable-release gate: pending.
- Signed tag attempt: blocked; GPG reported no secret key for the configured
  signing identity. No local `v2.2.0` tag was created and no remote write was
  attempted.

## Fail-closed limitations

The attestation and publication are not complete until the signed immutable
tag, remote refs, publisher verification, hosted security checks, and stable
release gate are independently verified. The consumer lock remains on the
previous verified release until those checks pass.

## Blocking remediation

Provision or select an approved signing identity with an available private key,
verify it without recording key material, and rerun the signed-tag verification
before attempting any remote publication.
