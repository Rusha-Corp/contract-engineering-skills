# CENG-T013-P002-EV001

Validator root and local mode implementation evidence. Root resolution is
explicit root, `CE_PROTOCOL_ROOT`, lock `project.protocol_root`, then the
`.contract-engineering` default. Relative roots resolve from the project
directory and absolute roots remain absolute. `off` skips without reading or
mutating records, `advisory` reports failures while returning success, and
`enforced` raises/returns failure. Missing roots never invoke Factory hooks.

Validation:

- `python3 -m unittest discover -s tests -p 'test_*.py'`: 54 tests passed.
- `python3 scripts/validate-contract-records.py --root .contract-engineering --mode enforced`:
  passed.
- `python3 scripts/validate-tracker.py --root .contract-engineering`: passed.
- `python3 scripts/render-tracker.py --root .contract-engineering --check`: passed.
Limitations: authoritative design/admission helpers are absent from the
locked checkout, as recorded by the parent decision. The Factory hook remains
out of scope.
