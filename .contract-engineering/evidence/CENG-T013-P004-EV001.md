# CENG-T013-P004-EV001

Implemented atomic transition and reassignment operations in
`scripts/contract_engineering.py`, with prepared/committed JSON journal records,
idempotent recovery, read-only doctor diagnostics, and explain-block output.

Rework fix: extended `recover()` to handle `kind == "reassign"` journal
records (previously only `kind == "transition"` was recovered). Added three
crash fault-injection tests that simulate a crash after journal write but
before packet update, then verify recovery completes the operation and that
repeated recovery is idempotent.

Validation:

- `python3 -m unittest tests.test_transitions -v`: passed, 7 tests:
  - test_transition_and_replay_are_idempotent: ok
  - test_illegal_completion_explains_gate: ok
  - test_reassign_requires_expired_or_released_lease: ok
  - test_doctor_is_read_only: ok
  - test_crash_recovery_completes_transition: ok (crash fault-injection)
  - test_crash_recovery_completes_reassignment: ok (crash fault-injection)
  - test_recovery_is_idempotent_after_replay: ok (idempotency proof)
- `python3 -m unittest discover -s tests -p 'test*.py'`: blocked by the
  pre-existing unrelated CENG-T004-P017 tracker/packet mismatch.
- Contract, tracker, and projection validation: blocked by that same unrelated
  mismatch.

The implementation is file-only and explicitly not split-brain safe; no remote
systems or external effects are used.
