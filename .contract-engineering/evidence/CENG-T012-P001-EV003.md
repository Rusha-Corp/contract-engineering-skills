# CENG-T012-P001-EV003

## Result

Remote CI correctly rejected the first corrective `v2.5.1` tag. The records
job detected drift in the generated archive projection. The stable gate
verified the tag but rejected the workflow's fingerprint comparison because
the parser selected the `VALIDSIG` label rather than the fingerprint token.

## Correction

- Do not rewrite the existing remote `v2.5.1` tag.
- Regenerate both Markdown projections with the table separator.
- Extract the fingerprint from token three of the `VALIDSIG` status line.
- Supersede the unverified tag with `v2.5.2`.

## Validation

The failed CI run is retained as release-gate evidence. The corrective release
must pass the full Python 3.12 workflow and signed-tag stable gate before it is
treated as verified.
