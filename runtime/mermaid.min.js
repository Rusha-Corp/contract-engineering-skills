/*! Contract Engineering offline Mermaid runtime pin: 11.4.1.
 * The preview uses this local asset only. CLI rendering remains fail-closed
 * when a verified host renderer is unavailable.
 */
(function (global) {
  "use strict";
  global.contractEngineeringMermaid = Object.freeze({
    version: "11.4.1",
    offline: true,
    renderAvailable: false
  });
}(window));
