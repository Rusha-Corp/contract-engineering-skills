# CENG-T004-P023-EV001

## Scope

- Update `README.md` so the repository landing page visibly shows the
  Contract Spark lockup and clearly explains what the project is before
  configuration details.
- Base: `9984587b4ed68f0b8df7a40d1a1b98ff4c56d3a1` (v2.4.0 protocol lock
  commit on main).

## README changes

### Branding (AC001)

The Contract Spark lockup is now rendered immediately below the title using
a local relative image reference:

```html
<img src="assets/contract-spark/contract-spark-lockup.svg"
     alt="Contract Spark, the Contract-Driven Engineering protocol identity"
     width="280">
```

- The image target is the existing local SVG at
  `assets/contract-spark/contract-spark-lockup.svg`.
- No remote image URL, JavaScript, or external dependency is introduced.
- Alt text is meaningful and describes the identity.

### Project overview (AC002)

The previous terse tagline was replaced with a structured overview that:

1. States the project name and purpose: a tool-agnostic protocol for making
   AI-assisted software development safe, accountable, and auditable.
2. Explains the core concept: bounded packets with owner, scope, gates,
   evidence, and independent handoff acceptance.
3. Provides a table listing what the repository supplies: governed skills,
   templates, host adapters, reference docs, visual identity, and CI
   workflows, each with its directory and purpose.
4. Identifies the intended audience: human engineers, coding assistants, CI
   automation, and agentic development environments.
5. Notes vendor independence and the lock-file adoption model.

### Existing sections preserved (AC003)

The Contents, Protocol configuration, Core loop, Droid installation, Other
agentic development environments, Updating and rollback, Visual identity,
and Validation sections all remain present and unchanged. Relative Markdown
links resolve.

### Scope adherence (AC004)

Changed paths are `README.md` and governed P023 records only. No SVG assets,
schemas, skills, workflows, protocol lock, or published tags were modified.

## Local validation

- `python3 scripts/validate-contract-records.py`: 41 packets, pass.
- `python3 -m unittest discover -s tests -p 'test_*.py'`: 38 tests, pass.
- `python3 scripts/check-markdown-links.py`: 30 files, pass.
- `python3 scripts/validate-svg-assets.py assets/contract-spark/`: 3 files, pass.
- `git diff --check`: pass.
- Local image check: 1 local `src="assets/contract-spark` reference, 0
  remote `src="http` references.

## Release tag and lock integrity

- `v2.2.0`, `v2.2.1`, `v2.3.0`, `v2.4.0` tags: unchanged.
- `.contract-engineering/protocol.lock.yaml`: unchanged.
