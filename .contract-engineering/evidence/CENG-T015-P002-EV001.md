# CENG-T015-P002-EV001

## Acceptance-first record validation

- `python3 -m unittest tests.test_acceptance_contracts tests.test_contract_records.AcceptanceContractTests`: exit 0, 15 tests passed.
- `python3 -m unittest tests.test_record_migration`: exit 0, 2 tests passed.
- `python3 -m unittest tests.test_planning_records`: exit 0, 2 tests passed.
- `python3 -m unittest discover -s tests -p 'test_*.py'`: exit 0, 84 tests passed.
- `python3 scripts/validate-contract-records.py --root .contract-engineering`: exit 0.
- `python3 scripts/validate-tracker.py --root .contract-engineering`: exit 0.
- `python3 scripts/render-tracker.py --root .contract-engineering --check`: exit 0.

The validator now permits empty criterion evidence during pre-completion states,
requires criterion evidence at `Complete`, validates criterion-linked structured
unknowns for new packets, preserves legacy string unknowns during migration, and
validates JSON Schema packet instances.
