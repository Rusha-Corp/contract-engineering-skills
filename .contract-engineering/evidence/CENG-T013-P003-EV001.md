# CENG-T013-P003-EV001

Validation evidence for the shared identifier and schema alignment packet.

This evidence is rebound for review against committed content head
`1b9f45ebbded578c62fc15ee9b5b88ee9fbe71a6`; the handoff record is excluded
from changed-resource hashing to preserve non-circular binding.

Supported identifiers:

- Legacy: `CENG-T001`, `CENG-T001-P001`
- Program-scoped: `RUSHA-PROGRAM-T001`, `RUSHA-PROGRAM-T001-P001`
- Multi-component program: `RUSHA-PLATFORM-CORE-T001-P001`

The canonical grammar is owned by `scripts/identifiers.py` and mirrored by
`schemas/identifiers.schema.json`; packet, dependency, tracker, event, and
evidence schemas reference that grammar. Lowercase components, empty
components, and incorrectly sized task or packet numbers are rejected.

Validation commands and results:

```text
python3 -m unittest discover -s tests -p 'test_*.py'
Ran 56 tests ... FAILED: pre-existing unrelated tracker mismatch
(`CENG-T004-P017` packet is `Rework` while the tracker index is `Handoff`).
The focused P003 identifier and acceptance-contract tests passed:

```text
python3 -m unittest tests.test_contract_records.CanonicalIdentifierTests tests.test_contract_records.AcceptanceContractTests
Ran 12 tests ... OK
```

python3 scripts/validate-contract-records.py --root .contract-engineering
contract validation valid (mode=enforced)

python3 scripts/validate-tracker.py --root .contract-engineering
tracker valid: 23 active rows, 24 archive rows, 2 YAML partitions

python3 scripts/render-tracker.py --root .contract-engineering --check
passed

python3 -m pip download --require-hashes --no-deps -r requirements-ci.txt -d /tmp/ci-requirements-check
Successfully downloaded PyYAML attrs jsonschema jsonschema-specifications referencing rpds-py

The full repository command was re-run after the record-only correction and is
blocked by that pre-existing unrelated tracker inconsistency; no unrelated
record was changed.
```

Limitations: the dependency check downloads Python 3.8-compatible cached wheels
into `/tmp`; it does not install them. No remote systems or Factory hooks were
accessed. Existing unrelated workspace changes were not modified.