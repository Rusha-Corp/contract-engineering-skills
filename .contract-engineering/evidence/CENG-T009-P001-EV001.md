# CENG-T009-P001-EV001

- Base revision: `8de326fe67e2aeb1c9918bf52b6549d97df062d8`.
- Protocol preflight passed against `$HOME/.factory/skills`.
- The repository CI workflows use Python 3.12, and the devcontainer uses
  `mcr.microsoft.com/devcontainers/python:1-3.12-bookworm`.
- The devcontainer JSON parser and required-settings checks passed.
- The host Factory skill directory is explicitly mounted read-only at
  /home/vscode/.factory. The previously planned read-write host `gh`
  workspace mount was removed per decision CENG-T009-P001-DEC001 because the
  host has no `~/gh` directory and no `gh` CLI installed.
- The container is configured for `linux/amd64` to match the CI runner and
  pinned dependency wheel environment.
- The post-create command uses the repository's
  `--require-hashes -r requirements-ci.txt` dependency policy.
- The Dev Container CLI was unavailable, so no CLI configuration parse or
  container build was run locally.
