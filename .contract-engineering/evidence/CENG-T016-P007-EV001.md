# CENG-T016-P007-EV001

- Packet: `CENG-T016-P007`
- Base revision: `b990693ebf4df11e85ac692bd932709a3365a271`
- Implementation revision: `a6d4456`
- Worktree: `/home/dev/contract-engineering-skills-CENG-T016-P007`

## Acceptance criteria

- CI validates actual epic/task/tracker/event instances against their normative
  JSON Schemas in addition to packet validation.
- CI derives changed paths from the submitted revision, requires a changed
  packet, and invokes packet-owned scope enforcement.
- CI reviews migration output in dry-run mode; mutation still requires explicit
  `--apply --confirm`.
- Existing terminal rows remain valid under the tracker’s terminal retention
  policy and are not rejected solely due to age.

## Validation

| Command | Result |
| --- | --- |
| `python3 -m unittest tests.test_ci_validation tests.test_record_migration -v` | exit 0, 5 tests passed |
| `python3 -m unittest discover -s tests -p 'test_*.py'` | exit 0, 106 tests passed |
| `python3 scripts/migrate_records.py --root .contract-engineering` | exit 0, deterministic dry-run |
| `python3 scripts/render-tracker.py --root .contract-engineering` | exit 0, projection current |
| `python3 scripts/validate-contract-records.py --root .contract-engineering` | exit 0, contract validation valid |
| `python3 scripts/validate-tracker.py --root .contract-engineering` | exit 0, tracker valid |

## Limitations

- Hosted GitHub Actions execution was not run locally. The workflow is covered
  by focused structural tests and uses the repository’s existing pinned-action
  security checks.
- The CI scope step intentionally fails revisions that change no claimed
  packet, enforcing packet ownership for repository changes.
