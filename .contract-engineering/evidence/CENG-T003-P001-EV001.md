# CENG-T003-P001-EV001

- Project lock parses and pins published commit `575f473` / release `2.1.0`.
- All five lock hashes match the corresponding files at `575f473`.
- YAML templates and tracked project records parse successfully.
- Markdown fences, relative links, terminology, and `git diff --check` pass.
- All documented bash blocks pass `bash -n`.
- Repository preflight passes against the synchronized Factory installation.
- Tampered hash preflight rejects drift.
- Existing ignored `.factory` records and unrelated Factory entries remain present.
