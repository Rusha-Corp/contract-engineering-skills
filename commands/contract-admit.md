# Contract Admit

Run the design, record, and packet admission validators. Show every blocked
reason and exit nonzero on failure.

Admission is fail-closed. A plan, review recommendation, or command result
cannot substitute for authenticated user approval.
