# CENG-T013-P002-EV001

## Scope and result

This evidence is bound to the packet's validator-root implementation. It
covers deterministic root precedence, explicit `off`, `advisory`, and
`enforced` modes, safe missing-root behavior, unit coverage, contract records,
tracker records, and the generated projection. The validator-root
implementation is unchanged by this rework. No Factory hook or `.factory-plugin`
file is read, changed, or invoked.

Actor: `worker`; capabilities: `repository-read`, `repository-write`,
`local-validation`; external effects: none. The exact commands below were run
from `/home/dev/contract-engineering-skills`; each command exited `0` unless
the output explicitly says `exit=1` for the expected enforced missing-root
case.

## Independently reproducible command output

### Root precedence and unit cases

```text
$ python3 -m unittest tests.test_contract_records.ValidatorRootAndModeTests
.....
----------------------------------------------------------------------
Ran 5 tests in 0.009s

OK
```

The five tests cover explicit-root > `CE_PROTOCOL_ROOT` > lock
`project.protocol_root` > `.contract-engineering`, absolute roots, `off`,
advisory missing-root, and enforced missing-root behavior.

### Explicit mode and missing-root behavior

```text
$ python3 - <<'PY'
import tempfile
from pathlib import Path
import importlib.util
spec = importlib.util.spec_from_file_location('v', 'scripts/validate-contract-records.py')
v = importlib.util.module_from_spec(spec); spec.loader.exec_module(v)
with tempfile.TemporaryDirectory() as d:
    p=Path(d)
    r=v.run_validation(p, mode='off')
    print(f'off: exit=0 mode={r.mode} valid={r.valid} skipped={r.skipped}')
    r=v.run_validation(p, explicit_root='missing', mode='advisory')
    print(f'advisory: exit=0 mode={r.mode} valid={r.valid} message={r.messages[0]}')
    try:
        v.run_validation(p, explicit_root='missing', mode='enforced')
    except ValueError as e:
        print(f'enforced missing-root: exit=1 error={e}')
PY
off: exit=0 mode=off valid=True skipped=True
advisory: exit=0 mode=advisory valid=False message=protocol root does not exist: /tmp/tmpbokhjb9p/missing
enforced missing-root: exit=1 error=protocol root does not exist: /tmp/tmpbokhjb9p/missing
```

The temporary directory name is process-generated; the behavior and exit
codes are the asserted contract.

### Full unit suite

```text
$ python3 -m unittest discover -s tests -p 'test_*.py'
...................................................tracker projections updated
.tracker valid: 1 active rows, 0 archive rows, 2 YAML partitions
tracker valid: 23 active rows, 24 archive rows, 2 YAML partitions
.
----------------------------------------------------------------------
Ran 54 tests in 0.804s

OK
```

### Contract-record, tracker, and projection validation

```text
$ python3 scripts/validate-contract-records.py --root .contract-engineering --mode enforced
tracker valid: 23 active rows, 24 archive rows, 2 YAML partitions
contract validation valid (mode=enforced)

$ python3 scripts/validate-tracker.py --root .contract-engineering
tracker valid: 23 active rows, 24 archive rows, 2 YAML partitions

$ python3 scripts/render-tracker.py --root .contract-engineering --check
```

The three commands each exited `0`; the projection check is silent on success.
The authoritative design/admission helper scripts remain absent from the
locked checkout, so no substitute helper was fabricated.
