# CENG-T013-P002-SEC001

Governed threat-model/design-gate record for P002. The structured decision
record is `.contract-engineering/decisions/CENG-T013-P002-SEC001.yaml`.

The actor is `worker` with only `repository-read`, `repository-write`, and
`local-validation` capabilities, under `CENG-T013-P001-DEC001`. The trust
boundaries are local root-selection inputs and the separate validator
boundary. Findings are THR-001 root tampering (high, mitigated and verified),
THR-002 unsafe missing-root handling (moderate, mitigated and verified), and
THR-003 accidental capability escalation (moderate, mitigated and verified).
No critical, high, or moderate finding remains unmitigated.

The Factory hook implementation and `.factory-plugin` are explicitly excluded;
no Factory hook was read, changed, or invoked. Evidence is
CENG-T013-P002-EV001, with verification recorded in
CENG-T013-P002-SEC002.
