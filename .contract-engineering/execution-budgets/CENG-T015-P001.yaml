budget_version: 1
packet_id: CENG-T015-P001
limits:
  max_duration_minutes: 60
  max_tool_calls: 80
  max_retries: 3
  max_cost_usd: 0
  max_file_writes: 40
  max_network_requests: 0
  max_processes: 8
  allowed_write_paths:
    - ".factory-plugin/plugin.json"
    - "CHANGELOG.md"
    - ".contract-engineering/"
  allowed_network_targets: []
  destructive_actions: require_two_person
cancellation:
  timeout_action: stop
  cancel_source: user
  checkpoint_ref: null
emergency_stop:
  trigger: "Any unapproved remote action, signing credential exposure, or verification mismatch."
  authority: user
  action: isolate
  recovery_approval_ref: CENG-T015-P001-DEC001
exhaustion:
  state: blocked
  evidence_ref: CENG-T015-P001-EV001
  user_notified: true
