# CENG-T012-P002-EV001

## Scope

Validation evidence for the v2.5.5 corrective release preparation at content
commit `1d08010dd6d9388350ac483020f6ec9717bce1a8`, using the new public
fingerprint `E0695C669AF8BD1ED298EF6EB2EECCB9F7AA535D`.

## Passed checks

- Installed governed skill hashes and versions match the locked v2.5.4
  protocol source.
- Gitleaks 8.24.3 history scan passed with no leaks.
- Gitleaks 8.24.3 working-tree scan passed with no leaks.
- The committed public key fingerprint matches the generated release
  fingerprint.
- Repository JSON schema metadata validation passed for four schemas.
- The consumer lock remained on v2.5.4 during content preparation.
- The v2.5.4 annotated tag resolves to `71bedf29f6f97c818c6a2ce129de559bfb6fb8ed`,
  and its direct content parent remains `80f1c21f9428324e07644484bdfdac3832bb49e9`.
- The private key and passphrase were not found in tracked files or the
  release worktree.

## Pending or unavailable checks

- Python-based `scripts/validate-contract-records.py`,
  `scripts/validate-tracker.py`, tracker projection verification, and the
  Python unit-test suite could not run because no Python runtime is installed.
- No corrective v2.5.6 tag has been created or pushed.
- No v2.5.6 hosted workflow verification has occurred.
- Independent worker review was attempted but the provider was unavailable;
  the user reviewed and approved the local content revision for signing.

## Hosted v2.5.5 result

The signed `v2.5.5` branch and tag were published after explicit user
authorization. GitHub registered the public key with key ID
`B2EECCB9F7AA535D`. The hosted protocol-validation run
`33895896779` failed in the records job because
`.contract-engineering/tracker/events/CENG-T012.yaml` was not ordered by
`occurred_at`. The v2.5.5 tag and its failed workflow evidence are preserved
unchanged. User approved corrective v2.5.6 publication under
`CENG-T012-P002-DEC002`.

## Security evidence policy

evidence_policy_version: 1
packet_id: CENG-T012-P002
classification: confidential
contains_sensitive_data: false
data_categories: [release-integrity, security-validation]
redaction:
  required: true
  performed: true
  reviewer: droid
  method: "Recorded hashes, fingerprints, and bounded results only; omitted private key and passphrase material."
secret_scan:
  required: true
  tool: "gitleaks 8.24.3"
  result: pass
access:
  allowed_roles: [repository-maintainer, release-reviewer]
  storage: repository
  encryption_at_rest: false
  encryption_in_transit: true
retention:
  class: extended
  retain_until: "2026-12-03"
  disposal_approval: ""
telemetry:
  raw_content_allowed: false
  content_capture_approval: null
incident_ref: null
