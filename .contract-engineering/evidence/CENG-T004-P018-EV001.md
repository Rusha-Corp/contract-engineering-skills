# CENG-T004-P018-EV001

## Gitleaks remediation validation

- Original hosted failure: run `33442439570`, history scan, one
  `generic-api-key` finding at
  `.contract-engineering/release-attestation.yaml:29`.
- Finding classification: the matched value is the public release-key
  fingerprint recorded for provenance, not a private key, credential, or
  passphrase.
- Pinned scanner: Gitleaks `8.24.3`, downloaded from the workflow's approved
  release and SHA-256 verified against the workflow pin.
- History scan after remediation: pass, 44 commits scanned, no leaks found.
- Working-tree scan after remediation: pass, no leaks found.
- Synthetic private-key negative fixture: rejected as required.
- Allowlist scope: exact public fingerprint and exact
  `.contract-engineering/release-attestation.yaml` path, combined with an
  `AND` condition.
- No private key, passphrase, or secret value was added to repository records.

## Remaining release gate

The remediation is local and requires a new commit and hosted workflow run.
The already published `v2.2.0` tag is not rewritten. Release verification
remains blocked until the remediation is published and hosted security checks
pass.
