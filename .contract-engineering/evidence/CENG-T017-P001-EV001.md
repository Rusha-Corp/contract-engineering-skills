# CENG-T017-P001-EV001

- Packet: `CENG-T017-P001`
- Baseline: signed `v2.7.0` tag at `b0ec963770d8a0106a26cc4d893cbd9417c322d4`
- Scope: documentation-only v2.7.1 release parity rule and clean publication records
- Status: local validation passed; release publication gates remain pending

## Local validation

- `python3 scripts/validate-contract-records.py` — exit 0; enforced contract validation valid.
- `python3 scripts/validate-tracker.py --root .contract-engineering` — exit 0; 24 active rows, 24 archive rows, 2 YAML partitions valid.
- `python3 scripts/check-markdown-links.py` — exit 0; 31 Markdown files valid.
- `python3 -m unittest discover -s tests -p 'test_*.py'` — exit 0; 73 tests passed.
- `git diff --name-only v2.7.0` — only generated tracker projection, tracker index, changelog, contributing guidance, and release-integrity guidance changed.
- The baseline’s 17 terminal tracker rows with stale review timestamps were refreshed without changing their states.

## Publication plan

- Content commit will be based directly on `v2.7.0`.
- The attestation commit will bind the content commit, reviewer acceptance, signed tag, GitHub Release, and asset hashes.
- The signed `v2.7.1` tag and non-draft GitHub Release will point to the same attestation commit.
- Release assets will be generated from that exact tag and their SHA-256 values recorded in the release handoff.
