# CENG-T005-P001-EV001

- Pinned `actions/checkout` and `actions/setup-python` to reviewed full commit
  SHAs, disabled checkout credential persistence, set shallow checkout, and
  constrained the runner and job timeout.
- Added explicit read-only `contents` permissions, same-ref concurrency
  cancellation, and a documented `pull_request` trust boundary with no
  secrets or privileged pull-request triggers.
- Added `requirements-ci.txt` with a PyPI SHA-256 hash for the Python 3.12
  Linux x86_64 PyYAML wheel, and enabled `pip --require-hashes` in CI.
- Added weekly Dependabot updates for GitHub Actions and pip dependencies.
- Added an embedded static guard that passes the secure workflow and rejects a
  mutable action tag and a missing timeout in negative cases.
- Validation passed: both workflow YAML files parsed; the embedded CI guard
  passed; the hash-pinned wheel was downloaded and verified for the runner
  target; and `scripts/validate-contract-records.py` reported 22 valid packets
  with the four changed paths within packet scope.
- The local pip implementation did not support `--dry-run`; the hash check used
  `pip download --require-hashes` in a temporary directory instead. No
  credentials, secrets, or sensitive run output were recorded.
