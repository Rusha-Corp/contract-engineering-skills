# CENG-T004-P021-EV001

## Release scope

- Target: stable `2.3.0` protocol release.
- Prior published release: `v2.2.1` at `ad45e4790cb381478e1847d44d16b6d7db462230`.
- Prior failed release: `v2.2.0` at `90f29dc75b2936f30ee09d7c171e69633333adfe` (immutable audit evidence).
- Base: `e7a7177f09946afc3137f4798b1cd406f015b1fe` (P020 commit).

## Acceptance hardening implementation

- `schemas/work-packet.schema.json`: added `acceptance_contract` with
  versioned structured criteria (id, statement, expected_result,
  verification_method, verification_command, evidence_refs, failure_result,
  validation_ref). Legacy `acceptance_criteria` string list retained.
- `templates/work-packet.yaml`: added `acceptance_contract` template block.
- `scripts/validate-contract-records.py`: added
  `validate_acceptance_contract` enforcing criterion IDs, duplicate detection,
  non-empty statement/expected_result/verification_method, non-empty
  evidence_refs, and validation_ref cross-references.
- `.factory/droids/contract-driven-handoff-acceptance-reviewer.md`: added
  structured acceptance evaluation instructions requiring
  criterion-by-criterion evidence review and rejection of aspirational or
  non-measurable criteria.
- `skills/phased-engineering-execution/SKILL.md`: documented acceptance
  contract fields and reviewer obligations; bumped to 2.4.0.
- `skills/cleanup-protocol/SKILL.md`: bumped to 2.3.0 with shard/compaction
  revision history.
- `skills/project-lifecycle/SKILL.md`: bumped to 2.3.0 with closure criteria.

## Release-gate correctness

- `.github/workflows/protocol-validation.yml`: stable gate now validates:
  - Tag format (v-prefixed, no hyphen for stable tags).
  - Tag release name matches attestation `subject.release`.
  - `GITHUB_SHA` (tag target) equals attestation `subject.commit`.
  - Provenance `source_commit` matches `subject.commit`.
  - Publisher identity, signing_system, and signature_ref are present.
  - Existing checks: independent approver, verification pass, active
    revocation status.
- `.github/workflows/secret-scanning.yml` and `security.yml`: already trigger
  on `v*` tags from P020.
- `docs/release-integrity.md`: documented the two-commit release model.

## Local validation

- `python3 scripts/validate-contract-records.py`: 39 packets, pass.
- `python3 -m unittest discover -s tests -p 'test_*.py'`: 38 tests, pass.
- Workflow YAML, immutable action refs, tag triggers, gate checks: pass.
- Gitleaks 8.24.3 history (49 commits) and working-tree scans: pass.
- `git diff --check`: pass.

## Skill version changes

- phased-engineering-execution: 2.3.0 -> 2.4.0
- cleanup-protocol: 2.2.0 -> 2.3.0
- project-lifecycle: 2.2.0 -> 2.3.0

## Publication boundary

This packet does not rewrite or republish `v2.2.1` or `v2.2.0`. The consumer
protocol lock update is a separate post-release change after v2.3.0 is
independently verified.
