# CENG-T005-P004-EV001

- Added conditional JSON Schema requirements for `CENG-T005` actor,
  capabilities, risk, approval, and external-effect fields, plus applicable
  execution-control references for active high-risk packets.
- Added validator enforcement for authenticated actor shape, high-risk approval
  policy, forbidden capabilities, structured/reversible declared effects, and
  evidence redaction, scanning, sensitive storage, encryption, and telemetry
  requirements.
- Added ten unit tests covering valid semantics, automatic high-risk approval,
  excessive capability, unlisted effect, unredacted restricted evidence, and
  all P005 parser-limit cases.
- Corrected the schema’s research-reference pattern to accept both existing
  `CENG-GAP-*` and approved `CENG-SEC-*` identifiers.
- Validation passed: 22 packets against JSON Schema, bounded contract-record
  validation, all 10 unit tests, Python compilation, and P004 scope checks.
- Completed pre-enforcement packets remain supported as a documented migration
  boundary; active and newly created security packets are fail-closed.
